

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/erase-horseracing-india-website",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;

